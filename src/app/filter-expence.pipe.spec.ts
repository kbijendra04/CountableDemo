import { FilterExpencePipe } from './filter-expence.pipe';

describe('FilterExpencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExpencePipe();
    expect(pipe).toBeTruthy();
  });
});
